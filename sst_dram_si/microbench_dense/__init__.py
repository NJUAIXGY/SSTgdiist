"""
Dense microbench harness (standalone from mesh_template).

This package provides a minimal entry that reuses the existing mesh builder
(`mesh_template.build`) but avoids the legacy classification task plumbing
(spike_data scanning + fixed 4x4 layer banner).
"""

